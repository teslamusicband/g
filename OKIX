Эта ошибка означает, что сертификат Artifactory не доверенный для Java. Вот несколько способов решения:

## 1. Добавить сертификат Artifactory в cacerts

Сначала получите сертификат:
```bash
# Получить сертификат с сервера Artifactory
openssl s_client -showcerts -connect your-artifactory-server:443 < /dev/null 2>/dev/null | openssl x509 -outform PEM > artifactory.crt
```

Затем добавьте его в keystore:
```bash
keytool -import -alias artifactory -keystore $JAVA_HOME/lib/security/cacerts -file artifactory.crt
```

## 2. Настроить IntelliJ IDEA

В IntelliJ IDEA:
1. **File → Settings → Build, Execution, Deployment → Build Tools → Maven**
2. В разделе "User settings file" укажите custom settings.xml
3. Добавьте в settings.xml:

```xml
<settings>
  <servers>
    <server>
      <id>your-artifactory-id</id>
      <username>your-username</username>
      <password>your-password</password>
    </server>
  </servers>
</settings>
```

## 3. Добавить JVM параметры в IntelliJ

**Help → Edit Custom VM Options** и добавьте:
```
-Djavax.net.ssl.trustStore=/path/to/cacerts
-Djavax.net.ssl.trustStorePassword=changeit
```

## 4. Временное решение (не рекомендуется для продакшена)

Добавьте в VM options:
```
-Dcom.sun.net.ssl.checkRevocation=false
-Dtrust_all_cert=true
```

## 5. Проверить цепочку сертификатов

```bash
# Проверить полную цепочку сертификатов
openssl s_client -showcerts -verify 5 -connect your-artifactory-server:443
```

Возможно, нужно добавить не только сертификат Artifactory, но и промежуточные сертификаты из цепочки.

## 6. Альтернативный подход через Maven

В pom.xml или settings.xml добавьте:
```xml
<repositories>
  <repository>
    <id>artifactory</id>
    <url>https://your-artifactory-server/artifactory/repo</url>
  </repository>
</repositories>
```

Какой именно Artifactory сервер вы используете? Это поможет дать более точные инструкции.
